# ASL-reader
ASL recognition using hand tracking and machine learning.

## Demo
- Real-time ASL alphabet prediction using webcam input
- Currently supports: A, B, C, L, Y
- Uses MediaPipe hand landmarks + Random Forest classifier